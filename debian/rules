#!/usr/bin/make -f
%:
	dh $@ --with python3

override_dh_auto_build:
	for po in translations/*/LC_MESSAGES/*.po; do \
	msgfmt "$$po" -o "$${po%.po}.mo"; \
	done

override_dh_auto_clean:
	rm -f translations/*/LC_MESSAGES/*.mo
	dh_auto_clean
